__pycache__/
*.pyc
*.pyo
.DS_Store
.venv/
venv/
*.swp

# Build output — generated by build.py, deployed via wrangler.
# Keep data/offers.json (source-of-truth between scraper and build) but ignore
# the rendered site/. GitHub Actions re-runs build on every cron tick.
site/.wrangler/
